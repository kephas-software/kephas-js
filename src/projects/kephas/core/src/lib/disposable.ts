/**
 * Provides the dispose() method.
 *
 * @export
 * @interface Disposable
 */
export interface Disposable {
    /**
     * Disposes the resources hold by the instance.
     *
     * @memberof Disposable
     */
    dispose(): void;
}